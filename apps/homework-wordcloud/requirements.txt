# 學生作業文字雲（HomeworkWordCloud）執行與打包所需套件
# GUI 用 Python 內建的 tkinter，不需要額外安裝。
openpyxl>=3.1
pypdf>=4.0              # 讀東華教務系統的選課名單 PDF（純 Python，不用 PyMuPDF）
python-pptx>=0.6.23
wordcloud>=1.9
jieba>=0.42
pillow>=10.0
numpy>=1.24
matplotlib>=3.7          # wordcloud 內部用它的色彩模組
pyinstaller>=6.6         # 只有打包 exe 時才需要
# 只有 Windows 需要：用 PowerPoint COM 匯出簡報縮圖（沒有也能跑，只是跳過縮圖）
pywin32>=306; sys_platform == "win32"
